---
import BaseHead from '../components/BaseHead';
import Footer from '../components/Footer';
import Header from '../components/Header';
import { SITE_DESCRIPTION, SITE_TITLE } from '../consts';

const canonicalUrl = new URL(Astro.url.pathname, Astro.site).toString();
const pageUrl = Astro.url.toString();
const rssUrl = new URL('rss.xml', Astro.site).toString();
const sitemapUrl = new URL('sitemap-index.xml', Astro.site).toString();
const generator = Astro.generator;
const currentPath = Astro.url.pathname;
---

<!doctype html>
<html lang="en">
	<head>
		<BaseHead
			title={SITE_TITLE}
			description={SITE_DESCRIPTION}
			canonicalUrl={canonicalUrl}
			pageUrl={pageUrl}
			rssUrl={rssUrl}
			sitemapUrl={sitemapUrl}
			generator={generator}
		/>
	</head>
	<body>
		<Header currentPath={currentPath} />
		<main class="mx-auto flex w-full max-w-content flex-col gap-6 px-4 py-16">
			<h1 class="text-4xl font-bold text-ink sm:text-5xl">
				<span aria-hidden="true" class="mr-2">🧑‍🚀</span>
				Hello, Astronaut!
			</h1>
			<p class="text-lg text-fog">
				Welcome to the official <a class="underline" href="https://astro.build/">Astro</a> blog starter
				template. This template serves as a lightweight, minimally-styled starting point for anyone
				looking to build a personal website, blog, or portfolio with Astro.
			</p>
			<p class="text-lg text-fog">
				This template comes with a few integrations already configured in your
				<code class="mx-1">astro.config.mjs</code> file. You can customize your setup with
				<a class="underline" href="https://astro.build/integrations">Astro Integrations</a>
				to add tools like Tailwind, React, or Vue to your project.
			</p>
			<div class="rounded-xl bg-white/70 p-6 shadow-soft backdrop-blur">
				<p class="text-lg font-semibold text-ink">Here are a few ideas to get started:</p>
				<ul class="mt-4 list-disc space-y-2 pl-6 text-base text-fog">
					<li>Edit this page in <code>src/pages/index.astro</code></li>
					<li>Edit the site header items in <code>src/components/Header.tsx</code></li>
					<li>Add your name to the footer in <code>src/components/Footer.tsx</code></li>
					<li>Check out the included blog posts in <code>src/content/blog/</code></li>
					<li>Customize the blog post page layout in <code>src/layouts/BlogPost.astro</code></li>
				</ul>
			</div>
			<p class="text-lg text-fog">
				Have fun! If you get stuck, remember to
				<a class="underline" href="https://docs.astro.build/"> read the docs</a>
				or <a class="underline" href="https://astro.build/chat">join us on Discord</a> to ask questions.
			</p>
			<p class="text-lg text-fog">
				Looking for a blog template with a bit more personality? Check out
				<a class="underline" href="https://github.com/Charca/astro-blog-template"> astro-blog-template</a>
				by <a class="underline" href="https://twitter.com/Charca">Maxi Ferreira</a>.
			</p>
		</main>
		<Footer />
	</body>
</html>
